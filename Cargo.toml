[package]
name = "aerofoil"
version = "0.1.0"
edition = "2021"

[dependencies]
rusteron-client = { version = "0.1", optional = true }

[features]
default = ["rusteron"]
rusteron = ["rusteron-client"]
