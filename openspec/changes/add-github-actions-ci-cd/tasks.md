# Tasks: Add GitHub Actions CI/CD

- [ ] Create `.github/workflows/rust.yml`
- [ ] Add a job for running tests with default features.
- [ ] Add a matrix job for running tests for `rusteron` and `aeron-rs` features individually.
- [ ] Add a step for linting the code with `clippy`.
- [ ] Add a step for checking formatting with `cargo fmt --check`.
- [ ] Ensure the workflow is triggered on pushes and pull requests to `main`.
